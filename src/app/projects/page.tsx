import React from 'react';

const ProjectsPage = () => {
    return (
        <div>
            this is the projects page. where i will show all the projects.
        </div>
    );
};

export default ProjectsPage;